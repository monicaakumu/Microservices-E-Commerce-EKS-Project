pipeline {
    agent any

    stages {
        stage('Clean Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout Source Code') {
            steps {
                git branch: 'main', url: 'https://github.com/monicaakumu/Microservices-E-Commerce-EKS-Project.git'
            }
        }

        stage('Docker Image Build') {
            steps {
                script {
                    // Navigate to the correct service directory
                    dir('src/checkoutservice') {
                        // Pruning commands are resource-intensive; consider using a dedicated clean agent
                        sh 'docker container prune -f'
                        sh 'docker build -t checkoutservice .'
                    }
                }
            }
        }

        stage('ECR Image Push') {
            steps {
                script {
                    sh '''
                    # Authenticate Docker client to ECR
                    aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 970547380044.dkr.ecr.us-east-1.amazonaws.com

                    # Tag the image with the unique Jenkins BUILD_NUMBER
                    docker tag checkoutservice:latest 481665089446.dkr.ecr.us-east-1.amazonaws.com/checkoutservice:${BUILD_NUMBER}

                    # Push the tagged image to ECR
                    docker push 481665089446.dkr.ecr.us-east-1.amazonaws.com/checkoutservice:${BUILD_NUMBER}
                    '''
                }
            }
        }

        stage('Update Git Deployment File') {
            environment {
                // Define environment variables
                GIT_REPO_NAME = "Microservices-E-Commerce-EKS-Project" // Corrected capitalization from previous version
                GIT_EMAIL = "moni.akumu@gmail.com"
                GIT_USER_NAME = "monicaakumu"
                IMAGE_NAME = "checkoutservice"
                REPO_URL = "481665089446.dkr.ecr.us-east-1.amazonaws.com/checkoutservice"
                YAML_FILE = "checkoutservice.yaml"
            }
            steps {
                // Navigate to the directory containing the Kubernetes YAML files
                dir('kubernetes-files') { 
                    withCredentials([string(credentialsId: 'my-git-pattoken', variable: 'git_token')]) {
                        sh '''
                        # Configure git user/email
                        git config user.email "${GIT_EMAIL}"
                        git config user.name "${GIT_USER_NAME}"

                        # Redundant assignment removed; BUILD_NUMBER is already available.
                        echo "Applying image tag: ${REPO_URL}:${BUILD_NUMBER}"

                        # Update the image tag in the YAML file
                        sed -i "s#image:.*#image: ${REPO_URL}:${BUILD_NUMBER}#g" ${YAML_FILE}

                        # Stage the specific modified file (safer than 'git add .')
                        git add ${YAML_FILE}

                        # Commit the change. Use '|| true' to IGNORE EXIT CODE 1 
                        # if the image tag was not actually changed and no commit is needed.
                        git commit -m "Update ${IMAGE_NAME} Image to version ${BUILD_NUMBER}" || true

                        # Push the commit using the secure token
                        git push https://${git_token}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME} HEAD:main
                        '''
                    }
                }
            }
        }
    }
}
